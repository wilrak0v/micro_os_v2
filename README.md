# Micro OS V2

## Graphic engine

rectangle = new pos et old pos

liste de commande = type + rectangle + couleur + old pos et new pos

on parcours la liste, pour chaque élément on vérifie si new pos != old pos

si c'est pas le cas on passe au prochain élément

Si c'est le cas on redessine ce nouvel élément.